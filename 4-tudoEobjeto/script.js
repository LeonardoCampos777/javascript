const nome = "Leo gatinho";

const nomeMaisculo = nome.toUpperCase();
console.log(nomeMaisculo);

function areaQuadrado(lado){
  return lado * lado;
}
areaQuadrado.toString();


const botao = document.querySelector('.botao');

botao.classList.add('ativo');